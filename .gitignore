node_modules/
dist/
data/*.json
data/*.tmp
.env
.env.*
npm-debug.log*
.DS_Store
Thumbs.db
data/*.txt
# contas: progresso de cada pessoa, usuários (hash de senha) e sessões — nunca no repo público
data/usuarios/

# transcricao pessoal do chat que originou o app (fica so local; o repo e publico)
HistoricoChat.txt
.claude/
.npmcache/
